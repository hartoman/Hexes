export class TileSetCreator{

    constructor (colorScheme, IconScheme) {
        this.colorScheme = colorScheme;
        this.IconScheme = IconScheme;
        this.tiles = null;
        this.tileSet = null;
    }

    getTileSet = () => {
        return this.tileSet;
    }

    createTilesetFromTiles = (tiles) => {
        const tileset = []

        return tileset;
    }

} 